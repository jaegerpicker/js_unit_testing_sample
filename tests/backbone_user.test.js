var chai = require('chai');
var sinon = require('sinon');
var backbone = require('backbone');
var $ = require('jquery');

describe('User', function() {
    describe('User posts to server', function() {
        it('should create a new user', function() {
            
        })
    });
});
